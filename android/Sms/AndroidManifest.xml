<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.navchina"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:label="Sms Test"
        android:targetPackage="com.navchina" >
    </instrumentation>

    <application
        android:name=".sms.SmsApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <uses-library android:name="android.test.runner" />

        <activity
            android:name=".sms.SmsActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".contact.ContactsActivity" >
        </activity>
        <activity android:name=".contact.EditContactActivity" >
        </activity>
        <activity android:name=".sms.InboxActivity" >
        </activity>
        <activity android:name=".sms.UnsendBoxActivity" >
        </activity>
        <activity android:name=".sms.SentBoxActivity" >
        </activity>
        <activity android:name=".sms.DraftBoxActivity" >
        </activity>
        <activity android:name=".sms.SmsDetailActivity" >
        </activity>
        <activity android:name=".sms.SmsWriteActivity" >
        </activity>
    </application>

</manifest>